# CLAUDE.md

## 이 레포는
개인 기술 블로그 (GitHub Pages). AI 오케스트레이션 토이프로젝트를 비롯한 개발 경험을 정리해 올리는 곳.

## 나(운영자)에 대한 배경
- 6년+ 프론트엔드 리드, Canvas/WebGL 렌더링·Fabric.js 커스텀 확장·TF 리드 경험
- 현재 구직 중, 방통대 AI전공 재학 중

## 블로그의 목적
1. **구직용 포트폴리오** — AI 오케스트레이션 역량(여러 AI 에이전트로 워크플로우 설계·운영)을 실무 사례로 증명
2. 이해 못 하던 개념을 깊게 탐구·정리하는 과정 자체를 콘텐츠화
3. 제3자가 봤을 때 전문성이 느껴지는 글 — "AI가 다 했다"가 아니라 "AI와 협업하며 내가 판단·설계했다"가 드러나야 함

## 글쓰기 페르소나 / 톤
- 담백하고 직설적. 과장된 수사("완벽히", "혁신적인", "압도적인") 금지
- 검증 안 된 사실·수치·성능 개선폭을 절대 지어내지 않음. 확실하지 않으면 "확실하지 않다"고 씀
- 없는 경험을 있다고 쓰지 않고, 묻지 않은 약점을 먼저 나서서 고백하지도 않음
- "표준 방법"이라 말할 때는 실제 근거(논문, 업계 관행, 공식 문서)를 댈 수 있을 때만 사용

## 글 구조 원칙
- 결과보다 과정 중심: 문제 발견 → 원인 진단 → 해결 → 왜 그렇게 판단했는지 근거
- 완성된 코드만 보여주지 말고, 처음 접근이 왜 틀렸는지도 포함 (막혔다가 뚫은 과정이 핵심 자산)
- 이론적 배경(예: 표준 방법론)과 직접 설계한 부분을 항상 구분해서 서술

## 작업 방식
- `_locals/` = 검토 대기 중인 글 초안을 두는 폴더 (Jekyll이 안 읽음, 발행 안 됨). 검토·수정이 끝나면 `_posts/YYYY-MM-DD-slug.md`로 옮기고 나서야 발행 대상이 된다.
- 글에 이미지가 필요하면 `assets/images/<글-slug>/` 폴더를 만들어 그 안에 둔다 (Jekyll이 `_posts/` 안의 파일은 static 파일로 안 취급해서 이미지를 못 넣음). 폴더명을 글 slug와 맞춰서, 글을 지울 때 같은 이름의 이미지 폴더도 같이 지우면 되게 한다.
- _posts/*.md 초안을 편집·리뷰하기 전에 .claude/agents/blog-style-editor.md 체크리스트를 먼저 읽고 반영
- 초안을 먼저 제시하고, 확인받은 뒤에만 반영 — 바로 파일 수정/발행하지 않음
- 편집 시 "왜 이렇게 바꿨는지" 근거를 짧게 함께 설명
- 게시 전 항상: 과장된 표현 있는지, 근거 없는 주장 있는지 자체 점검 후 보고

## 하지 말 것
- 검증 안 된 통계·수치 임의 생성
- 유추·추측을 사실처럼 서술 (확실하지 않으면 질문하거나 명시)
- 실제 겪지 않은 시행착오를 극적으로 꾸며 쓰기